/**
 * LessonType Enum
 * Represents the types of lessons.
 *
 * @enum {string}
 * @property {string} PUBLIC - Public lesson type.
 * @property {string} PRIVATE - Private lesson type.
 * @property {string} MIXED - Mixed lesson type.
 */
export enum LessonType {
  PUBLIC = "PUBLIC",
  PRIVATE = "PRIVATE",
  MIXED = "MIXED",
}
